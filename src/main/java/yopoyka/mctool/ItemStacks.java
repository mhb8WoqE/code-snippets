package yopoyka.mctool;

public class ItemStacks {
    /*
    public static void decrStackInSlot(net.minecraft.inventory.IInventory inv, int slot, int amount) {
        final net.minecraft.item.ItemStack stack = inv.getStackInSlot(slot);
        if (stack != null) {
            stack.stackSize--;
            if (stack.stackSize < 1) {
                inv.setInventorySlotContents(slot, null);
            }
        }
    }

    public static void decrStackInSlot(net.minecraft.item.ItemStack[] inv, int slot, int amount) {
        final net.minecraft.item.ItemStack stack = inv[slot];
        if (stack != null) {
            stack.stackSize--;
            if (stack.stackSize < 1) {
                inv[slot] = null;
            }
        }
    }
     */
}
